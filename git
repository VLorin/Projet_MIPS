Installer git

Verif:
git --version
git log

Compte:
git config --global user.name "Lorin Vincent"
git config --global user.email "vincent.lorin6@gmail.com"


Commit:
git add fileName
git commit -m "commentaire"

Branch:
git branch <nom>  : créer une branche
git branch : afficher les branche(s), * indique la branche actuel
git checkout <nom> : pour travailler sur une autre branche

Fusion branche:
git checkout master : où master sera la branche qui recevra les modifs
git merge <nom> : où <nom> sera la branche que l'on souhaite rapatrier
git branch -d <nom> : delete branche <nom>

Github:
git remote add origin https://github.com/VLorin/PX212_Ogar.git
git remote : vérifier
git push origin master : où origin est le label du dépot et master nom de la branche à déposer

Pour envoyer à github:
git add <file>
git commit -m "commentaire"
git pull origin master : master ou le nom d'une autre branche
git push origin master : idem

Pull : pour récuperer le projet
git pull origin master : où origin est le label du dépot et master nom de la branche à déposer